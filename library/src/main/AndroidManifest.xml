<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.proxyrack">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning"
            tools:targetApi="n"
            android:networkSecurityConfig="@xml/network_security_config">

        <activity android:name="net.monetizemyapp.android.PromptActivity"
                  android:screenOrientation="portrait"/>
        <activity android:name="net.monetizemyapp.android.MonetizationSettingsActivity"
                  android:screenOrientation="portrait">
        </activity>

        <service android:name="net.monetizemyapp.android.ProxyService"
                 android:stopWithTask="false"/>
        <!-- android:process=":ProxyService"/>-->

        <receiver android:name="net.monetizemyapp.android.BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>